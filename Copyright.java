import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Copyright here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Copyright extends Logo
{
    public Copyright()
    {
        GreenfootImage image = (GreenfootImage)getImage();
        image.scale(image.getWidth()/6, image.getHeight()/6);
        setImage(image);
    }
    
    /**
     * Act - do whatever the Copyright wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act()
    {
        
    }
}
